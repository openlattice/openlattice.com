/*
 * @flow
 */

import styled from 'styled-components';

import AnonymousSVG from './anonymous.svg';
import BarsSVG from './bars.svg';
import CheckBoardSVG from './checkboard.svg';
import CheckSVG from './check.svg';
import CloudSVG from './cloud.svg';
import CloudSecureSVG from './cloud-secure.svg';
import DBSyncSVG from './database-sync.svg';
import DotGridSVG from './dot-grid.svg';
import GavelSVG from './gavel.svg';
import KeySVG from './key.svg';
import LinkSVG from './link.svg';
import LockSVG from './lock.svg';
import LogsSVG from './logs.svg';
import MergeSVG from './merge.svg';
import OpenLatticeSVG from './ol-circle-icon.svg';
import PointerSVG from './pointer.svg';
import QuestionSVG from './question.svg';
import SearchSVG from './search.svg';
import SelectSVG from './select.svg';
import SyncSVG from './sync.svg';
import User1SVG from './user-1.svg';
import Users2SVG from './users-2.svg';
import Users2SolidSVG from './users-2-solid.svg';

const AnonymousCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'anonymous-circle-icon',
  src: AnonymousSVG,
  width: width || 64,
}))``;

const BarsCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'bars-circle-icon',
  src: BarsSVG,
  width: width || 64,
}))``;

const CheckBoardCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'checkboard-circle-icon',
  src: CheckBoardSVG,
  width: width || 64,
}))``;

const CheckCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'check-circle-icon',
  src: CheckSVG,
  width: width || 64,
}))``;

const CloudCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'cloud-circle-icon',
  src: CloudSVG,
  width: width || 64,
}))``;

const CloudSecureCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'cloud-secure-circle-icon',
  src: CloudSecureSVG,
  width: width || 64,
}))``;

const DBSyncCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'database-sync-circle-icon',
  src: DBSyncSVG,
  width: width || 64,
}))``;

const DotGridCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'dot-grid-circle-icon',
  src: DotGridSVG,
  width: width || 64,
}))``;

const GavelCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'gavel-circle-icon',
  src: GavelSVG,
  width: width || 64,
}))``;

const KeyCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'key-circle-icon',
  src: KeySVG,
  width: width || 64,
}))``;

const LinkCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'link-circle-icon',
  src: LinkSVG,
  width: width || 64,
}))``;

const LockCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'lock-circle-icon',
  src: LockSVG,
  width: width || 64,
}))``;

const LogsCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'logs-circle-icon',
  src: LogsSVG,
  width: width || 64,
}))``;

const MergeArrowCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'merge-arrow-circle-icon',
  src: MergeSVG,
  width: width || 64,
}))``;

const OpenLatticeCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'openlattice-circle-icon',
  src: OpenLatticeSVG,
  width: width || 64,
}))``;

const PointerCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'pointer-circle-icon',
  src: PointerSVG,
  width: width || 64,
}))``;

const QuestionCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'question-circle-icon',
  src: QuestionSVG,
  width: width || 64,
}))``;

const SearchCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'search-circle-icon',
  src: SearchSVG,
  width: width || 64,
}))``;

const SelectCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'select-circle-icon',
  src: SelectSVG,
  width: width || 64,
}))``;

const SyncCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'sync-circle-icon',
  src: SyncSVG,
  width: width || 64,
}))``;

const UserCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'user-1-circle-icon',
  src: User1SVG,
  width: width || 64,
}))``;

const Users2CircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'users-2-circle-icon',
  src: Users2SVG,
  width: width || 64,
}))``;

const Users2SolidCircleIcon = styled.img.attrs(({ width }) => ({
  alt: 'users-2-solid-circle-icon',
  src: Users2SolidSVG,
  width: width || 64,
}))``;

export {
  AnonymousCircleIcon,
  BarsCircleIcon,
  CheckBoardCircleIcon,
  CheckCircleIcon,
  CloudCircleIcon,
  CloudSecureCircleIcon,
  DBSyncCircleIcon,
  DotGridCircleIcon,
  GavelCircleIcon,
  KeyCircleIcon,
  LinkCircleIcon,
  LockCircleIcon,
  LogsCircleIcon,
  MergeArrowCircleIcon,
  OpenLatticeCircleIcon,
  PointerCircleIcon,
  QuestionCircleIcon,
  SearchCircleIcon,
  SelectCircleIcon,
  SyncCircleIcon,
  UserCircleIcon,
  Users2CircleIcon,
  Users2SolidCircleIcon,
};
