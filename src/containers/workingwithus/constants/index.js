/*
 * @flow
 */

const DATA_PIPELINES = 'We have created standardized data models for most key database types (e.g., RMS, SQL) to'
  + ' alleviate the challenges associated with bespoke data integrations. We reduce cost and complexity by letting'
  + ' users simply map fields onto the OpenLattice schema.';

const DATA_SHARING_AGREEMENTS = 'We recognize the challenges of establishing data sharing agreements among'
  + ' stakeholders, adhering to various laws and regulations, and navigating through data infrastructures. We gather'
  + ' data sharing agreements progressively, using a “Hub-Spoke-Wheel” model.';

const DEPLOY_APPS = 'Customers leverage one or more of our existing solutions, or develop their own on top of our open'
  + ' APIs, to intelligently link entities across their previously siloed datasets.';

const PROBLEM_DEFINITION = 'Identify a clear problem, determine the stakeholders, and envision your ideal solution.'
  + ' Whether it is countering a sudden rise in drug overdoses, reducing overcrowding in county jails, or the need for'
  + ' a shared case management interface across behavioral health agencies, we\'ll help you get there.';

export {
  DATA_PIPELINES,
  DATA_SHARING_AGREEMENTS,
  DEPLOY_APPS,
  PROBLEM_DEFINITION,
};
